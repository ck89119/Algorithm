#!/usr/bin/python

class Solution:
    # @param matrix, a list of lists of 1 length string
    # @return an integer
    def maximalRectangle(self, matrix):
     sum = [[0 for col in range(3)] for row in range(4)]
     print sum

ss = Solution()
ss.maximalRectangle([])
